{
  "name": "@tinkoff/request",
  "version": "0.6.0",
  "description": "Request library extendable by plugins",
  "private": true,
  "scripts": {
    "test": "jest --coverage",
    "bootstrap": "npm i && lerna bootstrap --force-local",
    "build": "lerna run tsc",
    "watch": "lerna run watch --parallel",
    "publish": "lerna publish",
    "clean": "lerna exec -- git clean -Xdf . && rm -rf node_modules"
  },
  "keywords": [
    "request"
  ],
  "license": "ISC",
  "devDependencies": {
    "@types/jest": "^24.0.11",
    "jest": "^24.7.1",
    "lerna": "^3.13.1",
    "prettier": "^1.16.4",
    "ts-jest": "^24.0.1",
    "typescript": "^3.4.1"
  }
}
